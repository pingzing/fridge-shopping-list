<Page
    x:Class="FridgeShoppingList.Views.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FridgeShoppingList.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:osk="using:TCD.Controls.Keyboard"
    mc:Ignorable="d">

    <Grid x:Name="ShellHost">
        <ContentPresenter x:Name="ShellContent"/>
        <Grid x:Name="CompositionEffectHost"                
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
              Visibility="Collapsed"/>
        <osk:OnScreenKeyBoard x:Name="AppSoftwareKeyboard" 
                              VerticalAlignment="Bottom"/>
    </Grid>
</Page>
